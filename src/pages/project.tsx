import { ContainerProject } from '@/components/project';
import { MainLayout } from '@/layout';

function project(): JSX.Element {
  return (
    <MainLayout>
      <ContainerProject />
    </MainLayout>
  );
}

export default project;
